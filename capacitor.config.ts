import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.chacha.starter',
  appName: 'Chacha Daig',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
